# Write a Python program that will accept the base and height of a triangle
# and compute the area


base = float(input("Enter base: "))
height = float(input("Enter height: "))

print("Area = ", base * height * 0.5)
